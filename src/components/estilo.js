import { StyleSheet } from "react-native";
//posso criar um arquivo de estilos separado e importar em qualquer componente que eu quiser.
export default StyleSheet.create({
    txtG: {
        fontSize: 24,
        textAlign: "center",
    }
});